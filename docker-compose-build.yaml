version: "3"
services:
  reverseproxy:
    build:
      context: ./udagram-reverseproxy
    image: hereiamken/reverseproxy
  backend_user:
    build:
      context: ./udagram-api-user
    image: hereiamken/udagram-api-user
  backend_feed:
    build:
      context: ./udagram-api-feed
    image: hereiamken/udagram-api-feed
  frontend:
    build:
      context: ./udagram-frontend
    image: hereiamken/udagram-frontend:local
